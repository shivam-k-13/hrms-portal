dependencies {
    compileOnly project(":modules:employee-service:employee-service-api")
    
    // CRITICAL FIX: Added 'leave-module' to the path to match your folder structure
    compileOnly project(":modules:leave-module:leave-service:leave-service-api")

    compileOnly group: "com.liferay.portal", name: "release.dxp.api"

    cssBuilder group: "com.liferay", name: "com.liferay.css.builder", version: "3.1.4"
}